# 第5回 HUSKYLENS + M5StickC PLUS2 外観検査

## 1. 今日の目標

今日の目標は、HUSKYLENS 単体で分類することではありません。

次の一連の流れを動かします。

```text
Button A
  ↓
HUSKYLENS が Class ID を返す
  ↓
M5StickC PLUS2 が OK / NG / RETRY を判断
  ↓
LCD 表示
  ↓
Relay Unit を ON / OFF
```

### 今日の最低到達点

- HUSKYLENS と M5StickC PLUS2 が I2C 通信できる
- HUSKYLENS から ID1〜ID4 を取得できる
- Button A を押すと検査が1回実行される
- OK / NG / RETRY に応じて LCD 表示と Relay Unit の ON / OFF が変化する

### 機材が用意できた場合の追加確認

適切な 24V DC 表示灯と 24V DC 電源が用意できた場合は、Relay Unit の接点で表示灯を ON / OFF します。

PLC を介した実際の FA ライン制御は、今回は扱いません。

---

## 2. 今回の製品仕様と学習クラス

製品仕様上の OK は次の2種類です。

- 青・青・青
- 黄・黄・黄

今回 HUSKYLENS に学習させるクラスは次の4種類です。

| HUSKYLENS ID | ワーク | M5側の判定 |
|---|---|---|
| ID1 | 青・青・青 | OK |
| ID2 | 黄・黄・黄 | OK |
| ID3 | 青・青・赤 | NG |
| ID4 | 黄・黄・赤 | NG |

重要：

> HUSKYLENS が直接「OK」「NG」を判断しているわけではありません。
>
> HUSKYLENS は Class ID を返し、その ID を M5StickC PLUS2 が OK / NG に対応付けます。

---

## 3. 作業の進め方

2人以上のグループでは、最初の作業を分担します。

- 1人：カメラ固定ジグを作る
- 1人：背景を準備する

手持ち無沙汰にならないよう、できる作業は並行して進めます。

### STEP 1 カメラ固定ジグを作る

次の条件を満たすように固定します。

- HUSKYLENS が動かない
- ワークの置き場所が決まっている
- 撮像距離が大きく変わらない
- ワークを一度外して戻しても、ほぼ同じ位置になる

目安の撮像距離は約 3 cm です。

**到達条件：ワークを置き直しても、ほぼ同じ構図で撮像できる。**

---

### STEP 2 背景を決める

背景候補を準備します。

まずは1色を選び、その背景で学習・認識を試します。

今回の4クラスが安定して認識できる場合は、背景比較に長い時間を使いません。

認識が不安定な場合に、もう1色へ交換して比較します。

**到達条件：学習・判定に使う背景を1色決める。**

---

### STEP 3 HUSKYLENS と M5StickC PLUS2 を配線する

| HUSKYLENS | M5StickC PLUS2 |
|---|---|
| VCC | 5V / 指定電源 |
| GND | GND |
| SDA | GPIO25 |
| SCL | GPIO26 |

配線後は、すぐに通電しないでください。

1. 自分で配線する
2. グループ内でもう1人が配線表と照合する
3. 教員に確認してもらう
4. 確認後に USB / 電源を接続する

<!-- 
**教員チェックポイント：通電前の配線確認**
-->

---

### STEP 4 I2C 接続を確認する

Thonny で次を実行します。

```python
from machine import Pin, I2C

i2c = I2C(
    0,
    sda=Pin(25),
    scl=Pin(26),
    freq=100000
)

print(i2c.scan())
```

次のように表示されれば成功です。

```text
[50]
```

`50` は HUSKYLENS の I2C Address `0x32` を10進数で表示したものです。

**到達条件：`[50]` が表示される。**

`[50]` が出ない場合は、この先へ進まず配線を確認してください。

---

## 4. HUSKYLENS の設定

次を確認します。

- Algorithm：`Object Classification`
- Learn Multiple：ON
- Threshold：70

Threshold をすぐに変更しないでください。

認識が不安定な場合は、先に次を確認します。

1. カメラ位置
2. 撮像距離
3. 背景
4. 照明
5. 再学習

それでも不安定な場合に Threshold を 65 / 70 / 75 程度で調整します。

---

## 5. 4クラスを学習する

HUSKYLENS の複数学習は操作が分かりにくいため、次の順番を守ってください。

### 最初に一度、以前の学習を消す

1. `Object Classification` モードにする
2. 学習ボタンを1回押し、以前の学習内容を忘れさせる

### ID1 を学習

3. 「青・青・青」をフレームに入れる
4. 学習ボタンを押し続けて ID1 として学習する

### ID2 へ進む

5. 学習後のカウントダウン中に、学習ボタンを1回押す
6. 次の ID を学習する待ち状態になったことを確認する
7. 「黄・黄・黄」をフレームに入れる
8. 学習ボタンを押し続けて ID2 として学習する

### ID3 / ID4 も同様

- ID3：青・青・赤
- ID4：黄・黄・赤

ID3 の学習後も、カウントダウン中に1回押して ID4 へ進みます。

### ID4 で終了するとき

ID4 を学習した後は、カウントダウン中にボタンを押しません。

そのままカウントダウンが終わるまで待ちます。

> ポイント：
>
> - 次の ID を覚えたい → カウントダウン中に1回押す
> - 学習を終わりたい → カウントダウン中は何も押さず待つ

<!--
**教員チェックポイント：ID1〜ID4が正しいワークに対応していること**
-->
---

## 6. 学習結果を確認する

次の4種類を順番に HUSKYLENS に見せます。

- 青・青・青 → ID1
- 黄・黄・黄 → ID2
- 青・青・赤 → ID3
- 黄・黄・赤 → ID4

ID の対応が違う場合は、M5 のプログラムへ進む前に HUSKYLENS を再学習してください。

### 認識精度の確認

ワークを一度取り外し、毎回置き直して判定します。

目標：

- 最低 8 / 10
- 目標 9 / 10 以上

単にワークを置いたまま連続して認識できるだけでは確認完了としません。

---

## 7. M5StickC PLUS2 の各機能を確認する

次を1つずつ確認します。

1. LCD に文字を表示できる
2. Relay Unit を ON / OFF できる
3. `husky.get_id()` で ID を取得できる
4. Button A の値を読み取れる

詳しいコードは `051_ai_learning_m5_connection.md` を参照してください。

**到達条件：4項目をすべて単体で確認できる。**

### Relay Unit について必ず理解する4点

Relay Unit を「5Vを24Vへ変換する装置」と考えてはいけません。

1. **Relay は昇圧器ではない。**
2. **M5 は Relay を ON / OFF しているだけで、24V の表示灯を直接駆動していない。**
3. **Relay の COM-NO 接点は、24V 側回路に入るスイッチとして働く。**
4. **M5 側の制御回路と、Relay 接点につないだ 24V 側回路は電気的につながっていない。**

つまり、24V は Relay が作るのではなく、別に用意した 24V 電源から供給します。

<!--
**教員チェックポイント：学生が「M5側」と「24V側」を別々の回路として指さして説明できること。**
-->

---

## 8. `main.py` を完成させる

`052_m5_main_template.py` を使います。

コード全体をそのままコピーして終わりにせず、特に次の部分を確認してください。

```python
if object_id in (1, 2):
    # OK
elif object_id in (3, 4):
    # NG
else:
    # RETRY
```

自分たちでコードとして完成させる部分は、主に次の3つです。

- LCD に何を表示するか
- 外部警告出力として Relay を ON / OFF のどちらにするか
- 未認識時をどう扱うか

### 完成後、説明できるようにする

教員から次の3点を確認します。

1. この `if` 文は何を判断しているか
2. HUSKYLENS が直接 OK / NG を出しているのか
3. 未認識時にも Relay を ON にして外部へ警告を出すのはなぜか

**到達条件：コードが動くだけでなく、上の3点を説明できる。**

---

## 9. 一連の検査を確認する

1. ワークを置く
2. Button A を押す
3. HUSKYLENS の ID を取得する
4. M5StickC PLUS2 が判定する
5. LCD 表示を確認する
6. Relay Unit の状態を確認する

期待する結果：

| HUSKYLENS ID | 表示 | Relay |
|---|---|---|
| ID1 / ID2 | OK | OFF |
| ID3 / ID4 | NG | ON |
| その他 / 未認識 | RETRY | ON |

重要：

> 「HUSKYLENS が分からない」と「製品として NG」は同じ意味ではありません。
>
> 今回は、NG と RETRY のときに Relay を ON にして、外部へ警告を出せる状態にします。

適切な 24V DC 表示灯と電源を用意できた場合は、Relay の COM-NO 接点を使って次も確認します。

- OK → Relay OFF → 表示灯 消灯
- NG → Relay ON → 表示灯 点灯
- RETRY → Relay ON → 表示灯 点灯

このとき、M5 から 24V が出ているわけではありません。24V 表示灯は別の 24V 電源で動作し、Relay 接点はその 24V 回路のスイッチとして働きます。

また、今回学習していないワークが必ず未認識になるとは限りません。既知の ID に誤分類される可能性もあります。

今回の4クラスだけで、製品仕様上のすべての NG を完全に検出できるわけではありません。

---

## 10. 困ったときの確認順序

全体を一度に変更しないで、次の順番で確認します。

1. M5StickC PLUS2 が Thonny から見えるか
2. LCD は表示できるか
3. Relay を ON / OFF できるか
4. HUSKYLENS の配線は正しいか
5. `i2c.scan()` で `[50]` が出るか
6. `husky.get_id()` で正しい ID が取れるか
7. Button A の値を読めるか
8. 最後に `main.py` の統合部分を確認する

---

## 11. 今日の終了条件

次を教員と確認して終了します。

- [ ] カメラとワーク位置が固定されている
- [ ] `i2c.scan()` で `[50]` が出る
- [ ] ID1〜ID4 の対応が正しい
- [ ] Button A で1回の検査を開始できる
- [ ] ID1 / ID2 で OK・Relay OFF になる
- [ ] ID3 / ID4 で NG・Relay ON になる
- [ ] 未認識時に RETRY・Relay ON になる
- [ ] 「Class ID → M5の判定 → Relay出力」の流れを説明できる
- [ ] Relay は昇圧器ではなく、別回路のスイッチであることを説明できる
- [ ] M5側回路と24V側回路が電気的につながっていないことを説明できる

片付け時間を確保し、終了10分前には新しい作業を始めないでください。

### 24V 表示灯を使用した場合のみ追加確認

- [ ] 24V 電源を外した状態では、Relay を ON にしても表示灯が点灯しない理由を説明できる
- [ ] M5 の GND と 24V 電源の 0V を共通接続しなくてもよい理由を説明できる

※ PLC の入出力、ラダー、PLC の先に接続された警告灯の制御は今回はスコープ外です。
